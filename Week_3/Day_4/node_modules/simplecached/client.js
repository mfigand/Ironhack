var Client = require('simplecached').Client;
var options = {
    port: 11511,
    host: '10.28.31.244'
};
var client = new Client(options, function(error){
  console.log("Client connected");
  var key = "Ironhack"
  var value = "We are ironhackers"

  client.set(key, value, function(error,result){
    if (error) console.log("Error setting the key");
    if (error) console.log("Key and value have been set");
    if (!error) console.log("The key and value could not bee set");
    client.get(Key, function(error, value){
      if (error){
        console.log("Error retrieving key");
      }
      else {
        console.log("Read ->",value);
        client.close();
      } 
    })
  })

});
