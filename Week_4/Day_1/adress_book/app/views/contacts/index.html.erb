<h1>This are the contacts</h1>

<ul>
  <% @contacts.each do |contact| %>
       <li><a href="/contacts/:id"> <%= "#{contact.name} - #{contact.adress} - #{contact.phone} - #{contact.email}" %> </a></li>
  <% end%>
</ul>

<a href="/contacts">Add new contact</a>
