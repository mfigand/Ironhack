<h1>This are the contacts</h1>

<ul>
  <% @contacts.each do |contact| %>
       <li><%= "#{contact.name} - #{contact.adress} - #{contact.phone} - #{contact.email}" %> </li>
  <% end%>
</ul>

<a href="/contact">Add new contact</a>
